# ═══════════════════════════════════════════════════════════════
# OpenClaw Control Plane Lite — Environment Configuration
# ═══════════════════════════════════════════════════════════════

# ── Gateway connection ──
OPENCLAW_GATEWAY_URL=ws://127.0.0.1:18789
OPENCLAW_GATEWAY_TOKEN=
# OPENCLAW_GATEWAY_PASSWORD=

# ── WSL support (Windows users running OpenClaw in WSL) ──
# Auto-detected on Windows. Set to "true" to force, "false" to disable.
OPENCLAW_USE_WSL=true
# Optional: specify WSL distro name (defaults to the default distro)
# OPENCLAW_WSL_DISTRO=Ubuntu

# ── Path overrides (usually auto-detected) ──
# OPENCLAW_CONFIG_PATH=        # e.g. \\wsl$\Ubuntu\home\user\.openclaw\openclaw.json
# OPENCLAW_STATE_DIR=          # e.g. \\wsl$\Ubuntu\home\user\.openclaw
# OPENCLAW_CRON_DIR=           # e.g. \\wsl$\Ubuntu\home\user\.openclaw\cron

# ── Adapter service ──
ADAPTER_PORT=3005
ADAPTER_HOST=127.0.0.1

# ── Frontend ──
NEXT_PUBLIC_ADAPTER_URL=http://127.0.0.1:3005
