{
  "name": "openclaw-control-plane-lite",
  "version": "1.0.0",
  "private": true,
  "description": "OpenClaw Control Plane Lite — a user-friendly dashboard for managing your OpenClaw Gateway",
  "scripts": {
    "dev": "concurrently -n adapter,web -c blue,green \"pnpm --filter @ocpl/adapter dev\" \"pnpm --filter @ocpl/web dev\"",
    "build": "pnpm --filter @ocpl/types build && pnpm --filter @ocpl/adapter build && pnpm --filter @ocpl/web build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r exec rimraf dist .next node_modules/.cache"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "rimraf": "^6.0.1",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
